record(catalog,
       {
	 service_id,
	 source
       }).
record(nodes,
       {
	 node_id,
	 node
       }).
record(service_discovery,
       {
	 service_id,
	 node,
       }).
record(deployment,
       {
	 service_id,
	 node
       }).
